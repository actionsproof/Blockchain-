[package]
name = "consensus"
version = "0.1.0"
edition = "2021"

[dependencies]
types = { path = "../types" }
crypto = { path = "../crypto" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"
